# Security Policy

`logfmt-tool` reads log text locally and makes no network calls. It parses input with a
hand-written tokenizer and never executes input data.

- Logs may contain sensitive data; handle inputs and any JSON output under your own
  data-governance rules.

## Reporting

Email **amarel.taylor.s@gmail.com** for any security concern rather than opening a public
issue.
